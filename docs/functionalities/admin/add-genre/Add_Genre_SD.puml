    @startuml

    title  "As an Administrator, I want to add a publication genre to the official \ngenre list, so that new publications can be of this genre" - SD

    '==== basic style ====
    skinparam monochrome false
    skinparam shadowing false
    skinparam defaultFontName Futura
    skinparam objectFontSize 20
    skinparam backgroundColor CornSilk
    skinparam objectBackgroundColor WhiteSmoke
    skinparam noteBackgroundColor #FFFFE0
    skinparam ArrowFontColor #3B4953
    skinparam ArrowColor #57595BAA
    skinparam ActivationBackgroundColor WhiteSmoke
    hide footbox

    autonumber

    actor Administrator
    participant ":AddGenreUI" as UI
    participant ":AddGenreController" as Ctl
    participant ":GenreFactory" as gf
    participant ":MemGenreRepo" as gr


    Administrator -> UI: I want to add a publication genre
    activate UI
    UI --> Administrator: What is the genre name?
    deactivate UI
    Administrator -> UI: genreName
    activate UI

    UI -> Ctl: addGenre(genreName)
    activate Ctl
    Ctl -> gf: createGenre(genreName)

    activate gf
    create "genreName:Genre"
    gf -->> "genreName:Genre": <<Create>>

    gf --> Ctl : genreName
    deactivate gf


    Ctl -> gr : isRepeated = containsOfIdentity(genreName.identity())
    activate gr
    opt !isRepeated
    Ctl -> gr: save(genreName)
    gr --> Ctl: genreName
     deactivate gr
    end


    Ctl --> UI: genreName
    deactivate Ctl

    UI --> Administrator: genreName added successfully \nto the official genre list!
    deactivate UI

    @enduml